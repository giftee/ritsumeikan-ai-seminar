# AGENTS.md

このファイルは、AIコーディングツール（GitHub Copilot / Cursor / Claude Code / Codex / Gemini CLI など）が自動で読み込む、このリポジトリの開発ルールです。

## このリポジトリについて

研究室・サークルの差し入れeギフト推薦を題材にした学習用教材です。仕様の正はGitHub Issueにあります。Issue本文の症状だけでなく、完了条件まで読んでください。

## 作業ルール

- いきなり実装しない。まず、仕様の理解、決まっていない点、修正方針、テスト方針を出し、承認を待つ
- 仕様として決まっていない点は、推測で埋めず、質問として列挙する。各質問には選択肢と推奨案を添える
- 推薦ロジックの変更は `src/giftRecommendation.js` と `test/giftRecommendation.test.js` の2ファイルに限定する
- 外部API連携、個人情報の保存、AIによる自由文メッセージ生成は追加しない
- 推薦ロジックでは、好みの一致だけでなく、予算、在庫、除外カテゴリ、同点時の決め方をテストで確認する
- バグ修正では、まずバグを再現するテストを書き、それが落ちることを確認してから実装する
- 変更後は `npm test` を実行し、結果を報告する
