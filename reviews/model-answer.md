# 模範回答

## AI依頼文の模範

あなたはペアプロ相手です。
GitHub Issue #1 の修正方針を考えたいです。
まず実装せず、仕様の理解、不明点、修正方針、追加すべきテスト、PRで説明すべき点を出してください。

## GitHub上の文脈

- Issue: 研究室・サークルの差し入れeギフトを条件に合わせておすすめする
- 対象ファイル: `src/giftRecommendation.js`
- 既存テスト: 候補がない場合のフォールバックのみ
- 完了条件: 仕様を満たす実装、予算・在庫・除外カテゴリ・好みタグ一致数・同点時の安い順のテスト、GitHub Actions通過、PR説明

## 目的

研究室・サークルの差し入れeギフトを、予算、在庫、好み、除外カテゴリに合わせて1つおすすめしたい。

## 制約

- 外部API連携、個人情報の保存、AIによる自由文メッセージ生成は今回の範囲外
- 予算内、在庫あり、除外カテゴリ以外、好みタグ1つ以上一致のギフトだけを候補にする
- 好みタグの一致数が多い順、一致数が同じなら安い順に選ぶ
- 差分は `src/giftRecommendation.js` と `test/giftRecommendation.test.js` に限定する

## Plan Gate判断

判断: Approve

理由:

- 予算、在庫、除外カテゴリ、好みタグ一致数を扱う方針になっている
- 一致数が多い順、同点時に安い順という優先順位を拾えている
- 候補がない場合のフォールバックも仕様として拾えている
- 外部API連携、個人情報保存、自由文生成を範囲外としている
- 追加テストに制約違反を防ぐ観点が含まれている

追加で確認したいこと:

- 実際のPRで、除外カテゴリのギフトが好みに合っていても選ばれないケースがテストに入っているか

## Review判断

判断: Request changes

理由:

CIは通っているが、今回の制約違反に直結する除外カテゴリのケースがテスト差分から読み取りづらい。
PR本文で「除外カテゴリ」をレビュー観点に挙げているため、対応するテストを1つ追加したい。
